<!--
0단계(요구사항 정렬) 산출물 템플릿. 프로젝트 폴더로 복사해서 채운다.
빈 템플릿입니다 — 아래는 전부 TODO/형식 예시이며 실제 내용이 아닙니다.

파일 간 분업:
  core-problem.md = 문제가 무엇인가 (핵심 사용자·핵심 문제·제약 D·결정지점 DP)
  brief.md        = 그래서 어느 방향인가 (반응 원문·역추출한 방향성 제약·가정·안티슬롭 제약)
  DESIGN.md       = 그래서 값이 얼마인가 (B-4 산출물. 토큰·모바일 제약·A단계 기준값의 정본)
**값은 이 파일에 쓰지 않는다.** "뉴트럴은 웜 계열"은 여기, `#F7F5F2`는 DESIGN.md다.
0단계에서 팔레트를 확정하면 B-1의 비주얼 무드 축이 시작부터 죽는다.

이 하네스는 모바일 하네스다. 폼팩터는 B단계의 축이 아니라 여기서 이미 정해진 제약이다.
-->

# Brief — TODO(프로젝트명)

> 0단계 산출물. 요구사항 문서: `TODO`
> 산출 파일(Figma 등): `TODO`
> **산출물 유형**: TODO — `랜딩` / `툴` / `혼합` 중 하나.
> 슬롭 게이트가 이 값으로 랜딩 전용 항목(히어로·헤드라인·통계 배너 등)을 적용할지 결정한다.
> 모바일에서는 `툴` = 앱의 기능 화면(목록·상세·입력·설정), `랜딩` = 온보딩·프로모션처럼
> 설득이 목적인 화면이다. 둘 다 있으면 `혼합`으로 두고 프레임 단위로 갈라 판정한다.
> `툴`인데 `랜딩`으로 적으면 없는 개념을 두고 오탐이 쏟아진다.

## 문제의 재정의

요구사항 문서가 한 덩어리로 말하는 어려움을, **성격이 다른 문제로 쪼갠다.** 성격이 다르면 푸는 화면도 다르다.

| 문제 | 성격 | 이 문제를 푸는 화면 |
|---|---|---|
| TODO | TODO (예: 집합 분할 / 스케줄 매칭 / 충돌 감지) | TODO |

**기존 방식이 실패하는 원인**: TODO
→ 그래서 이 제품의 핵심 화면은 TODO여야 한다.

## 자극과 반응

보여준 자극과 받은 반응을 **원문 그대로** 남긴다. 요약하면 역추출 근거가 사라진다.

| # | 보여준 자극 | 반응 원문 | 역추출한 판단기준 |
|---|---|---|---|
| 1 | TODO | "TODO" | TODO |

<!--
클라이언트가 없어 폴백(요구사항 문서의 상황 서술을 반응으로 간주)을 썼다면, 위 표의
"보여준 자극" 열에 자극 대신 **인용한 문장의 출처(§n)**를 적고, 역추출한 기준마다
ASSUMP-nn을 단다. 문서가 말하지 않은 것은 이 방법으로 드러나지 않기 때문이다.
-->

## 명시적 가정 로그

> 정본은 `core-problem.md` §5. 여기에는 **화면 설계에 직접 쓰인 것만** 옮겨 적는다.

1. **플랫폼**: 모바일 (고정). TODO — iOS / Android / 크로스플랫폼 중 무엇으로 좁혔고 근거는 무엇인가. 아래 "모바일 사용 맥락"에서 상술한다
2. TODO
3. TODO

## 안티슬롭 제약

`context/ai-slop.md`가 B-4의 `SL0` 게이트와 A·C 판정에서 이 블록을 읽는다. 채우지 않으면
하네스가 **남의 기본 팔레트로** 이 프로젝트를 판정하게 되고, 의도해서 고른 색이 슬롭으로 오인된다.

**0단계 종료 조건이기도 하다** — 이 블록이 비어 있으면 B단계로 넘어가지 않는다. `SL0` 자체는
값이 생긴 뒤(B-4)에 돌지만, 여기가 비면 그때 가서 `underspec`으로 되돌아올 것이 이미 정해져 있다.
아래 금지 id는 B-4에서 `DESIGN.md`의 `## Do's and Don'ts`로 승격된다 (`DS-03`).

- **금지하는 슬롭 id**: TODO (예: `color.purple-blue-gradient`, `layout.icon-top-3-cards`)
- **금지의 근거**: TODO — 위 "자극과 반응" 표의 어느 반응 원문에서 나온 금지인지. 근거 없는 금지는 취향이지 제약이 아니다.
- **의도적으로 허용하는 슬롭 id + 사유**: TODO (브랜드색이 실제로 인디고라면 여기에 근거를 남긴다. 그러면 `color.indigo-500-accent`가 발동하지 않는다)
- **오버라이드 불가**: `surface.low-contrast-body`(본문 대비율 4.5:1). 접근성 하한선은 협상 대상이 아니다.

## 역추출한 방향성 제약

**여기는 방향이고, 값은 `DESIGN.md`다.** "뉴트럴은 웜 계열로"는 0단계가 반응에서 뽑은 제약이므로
여기 쓰고, `#F7F5F2`는 B단계가 그 제약 안에서 고른 해이므로 `DESIGN.md`에 쓴다.
같은 값을 두 파일에 쓰면 갈라지는 순간 어느 쪽으로 판정할지 정할 수 없다.

| 축 | 방향 | 근거 반응 원문 | 하류에서 이 제약을 받는 곳 |
|---|---|---|---|
| 색 | TODO (예: 뉴트럴은 웜 한 방향 · `VC-01`) | "TODO" | `DESIGN.md ## Colors` |
| 정보 밀도 | TODO (예: 한 화면에 3~5개, 그 이상은 접기) | "TODO" | `DESIGN.md ## Layout` |
| 톤 | TODO | "TODO" | `DESIGN.md ## Overview` |
| TODO | TODO | "TODO" | TODO |

근거 반응 원문이 없는 줄은 제약이 아니라 취향이다. `ASSUMP-nn`으로 강등한다.

## 모바일 사용 맥락

`DESIGN.md`의 `mobile:` 블록 값을 가르는 입력이다. 여기가 비면 B-4에서 그 여섯 값을
근거 없이 정하게 되고, `DS-01`(근거 추적)에 걸린다.

- **플랫폼 판단 근거**: TODO — iOS / Android / 크로스플랫폼 중 무엇이고, 요구사항 문서의 어디서 나왔는가
- **손 상황**: TODO — 한 손인가 두 손인가. 이동 중인가 앉아서인가. 주요 동작을 화면 어느 대역에 둘지가 여기서 갈린다
- **조명·환경**: TODO — 실내 위주인가 야외가 섞이는가 (대비 하한과 다크모드 필요성)
- **세션 길이**: TODO — 몇 초 쓰고 닫는가, 몇 분 머무는가 (정보 밀도와 스크롤 길이)
- **기기 하한**: TODO — 지원해야 하는 가장 좁은 폭 (→ `mobile.minViewportWidth`)

## 디자인 토큰 값

**이 파일에 없다.** 모든 토큰 값·모바일 고정 제약·A단계 체크 기준값의 정본은
B-4 산출물인 **`DESIGN.md`**(템플릿: `templates/DESIGN.md`)다.

0단계에서 팔레트와 서체를 확정하면 B-1의 "비주얼 무드" 축이 시작부터 죽는다. 0단계의 산출은
**제약**이고 B단계의 산출이 **해**라는 분업이 이 파일의 존재 이유이므로, 값은 여기 두지 않는다.

- 값을 고를 때 지켜야 할 방향 → 위 "역추출한 방향성 제약"
- 실제 값 (`colors` / `typography` / `spacing` / `rounded` / `mobile` / `harness`) → `DESIGN.md` front matter
- A단계 `SC` 9개의 기준값 → `DESIGN.md`의 `harness:` 블록과 front matter
- 작성 규칙 → `context/design-system-doc.md`의 `DS-01`~`DS-07`
