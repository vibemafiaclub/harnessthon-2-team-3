<!--
B단계(발산·수렴) 산출물 템플릿. 프로젝트 폴더로 복사해서 채운다.
빈 템플릿입니다 — 아래는 전부 TODO/예시이며 실제 내용이 아닙니다.
-->

# Decisions

## 식별된 독립 축

- TODO: 축 1 (예: 비주얼 무드)
- TODO: 축 2 (예: 모바일 뷰포트 안에서의 정보 밀도)
- TODO: 축 3

**폼팩터는 축이 아니다.** 이 하네스의 산출물은 모바일 화면이고, 그건 0단계에서 이미 정해진
제약이다. "데스크톱 ↔ 모바일"을 축으로 세웠다면 축을 잘못 뽑은 것이다.

(축마다 `references.md`에 서로 다른 출처 2개 이상, 그중 1개는 다른 카테고리의 레퍼런스가 있어야 한다.)

(축을 나누지 않고 화면 전체를 통째로 비교하면, 클라이언트가 무엇 때문에 선택했는지 뒤섞여 판단 근거가 안 남는다.)

## 축별 후보·비교·선택

### 축: TODO

| 후보 | 설명 | 근거 ref | 비교 근거 | 공유 슬롭 id | 선택 여부 |
|---|---|---|---|---|---|
| 후보 1 | TODO | ref-01 | TODO | | |
| 후보 2 | TODO | ref-03, ref-05 | TODO | | |

<!--
"공유 슬롭 id" — 이 축의 후보들이 **공통으로 걸리는** ai-slop.md 항목을 적는다.
비어 있는 것이 정상이다. 값이 채워졌다면 그 축은 분리에 실패한 것이므로 축을 다시 나눈다.
후보 3개가 전부 layout.centered-hero라면, 그건 "무드" 축이 아니라 색만 바꾼 것이다.
-->

**선택 이유**: TODO

<!-- 이 줄이 비면 A단계에서 meta.mean-best-aesthetic(strong)이 확정된다. 결정을 안 한 것 자체가 슬롭이다. -->


**수렴 게이트**: 이 선택은 `references.md`의 어떤 메커니즘에서 왔고, 그 메커니즘이 우리 페르소나에서도 성립하는 이유는? → TODO
(답이 안 나오면 근거 없는 선택이다. 후보를 다시 만들거나 레퍼런스를 더 소싱한다.)

## 축 → `DESIGN.md` 반영 점검 (`DS-04`)

B-4에서 각 축의 선택이 실제로 어느 섹션의 값으로 내려앉았는지 확인한다.

| 축 | 내려앉은 `DESIGN.md` 섹션 | 구체적으로 어느 값 |
|---|---|---|
| 축 1: TODO | TODO (예: `## Colors`) | TODO |
| 축 2: TODO | TODO | TODO |

- **어느 섹션에도 안 걸린 축**은 화면에 영향을 주지 못한 축이다. 축을 잘못 세웠거나 수렴이 실질적으로 일어나지 않은 것이므로, 사유를 적고 축을 다시 나눈다. A단계에서 `meta.mean-best-aesthetic`의 확정 신호로도 쓰인다.
- **한 섹션에 여러 축이 몰렸으면** 그 축들은 독립이 아니었을 가능성이 높다.

미반영 축과 사유: TODO

## 남은 트레이드오프

- TODO

## 슬롭 게이트 상태

판정 결과 자체는 A·C 리포트에 `[슬롭 id] 불합격 …` 줄로 남긴다. **여기에는 회차를 넘어 살아남아야 하는 것만** 적는다.
이걸 안 남기면 "3회 연속 재발"을 셀 수 없어 에스컬레이션 규칙이 죽는다.

**시도 회차** (게이트당 상한 3회)

| 게이트 | 실행 시점 | 회차 | 결과 | 라우팅 |
|---|---|---|---|---|
| `SL0` | B-4 종료 · `DESIGN.md` 대상 | 1 / 3 | TODO 통과 · 실패 | |
| `SL-A` | A단계 · 노드 덤프 | 1 / 3 | | |
| `SL-C` | C단계 · 스크린샷 | 1 / 3 | | |

**weak 뭉침 판정** — 같은 프레임 안 `weak` 2건 이상은 `strong` 1건으로 승격된다.

| 프레임 | weak 확정 id | 건수 | 승격 |
|---|---|---|---|
| TODO | | | |

**3회 연속 재발한 id** — 후보를 바꿔도 계속 나오는 것. 상한과 무관하게 즉시 0단계 에스컬레이션.

| 슬롭 id | 재발 회차 | 어느 `ASSUMP-nn`을 의심하는가 |
|---|---|---|
| TODO | | |

**`DESIGN.md` 버전 이력** — C 실패로 B에 회귀해 후보를 바꾸면 `DESIGN-v2.md`로 새로 쓰고 원본을 덮어쓰지 않는다.
무엇이 바뀌었는지가 남지 않으면 "후보를 바꿔도 같은 슬롭이 3회 재발"을 셀 수 없다.

| 버전 | 계기 (어느 판정에서 내려왔나) | 바뀐 섹션·토큰 | 재발 여부 |
|---|---|---|---|
| `DESIGN.md` | 최초 (B-4) | — | — |
| TODO | TODO | TODO | TODO |

**사람에게 넘긴 항목**: TODO
